{
	"name": "innosoon",
	"version": "0.0.0",
	"private": true,
	"scripts": {
	  "build": "node build.js",
	  "build:watch": "nodemon --watch src_assets --ext js,css,html --exec \"npm run build\"",
	  "dev": "npm run build && wrangler dev --host template.dev",
	  "deploy": "npm run build && wrangler deploy"
	},
	"devDependencies": {
	  "@cloudflare/vitest-pool-workers": "^0.8.19",
	  "esbuild": "^0.25.4",
	  "glob": "^10.2.7",
	  "html-minifier-terser": "^7.0.0",
	  "nodemon": "3.1.10",
	  "vitest": "~3.1.3",
	  "wrangler": "^4.14.2"
	}
  }
  
  